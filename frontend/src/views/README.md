# views

Tab-level routed views (SPEC §7.1).

탭 단위 화면.

## Planned contents

- `SimView` — Three.js (React Three Fiber) 2.5D replay: tick interpolation, play/speed/zone jump; read-only view, lazy-route isolated, 2D canvas fallback for low-end devices (§7.5)
- `ReportView` — per-session report + narration (§9)
- `SettingsView` — LLM path selection (on-device ↔ external), BYOK key entry, per-feature toggles, first-activation disclosure (§9.1)
