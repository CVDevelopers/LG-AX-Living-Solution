# datasets (gitignored)

Landing spot for raw open-dataset downloads used by `simulator/calibrate.py` (SPEC §2.5). Contents are **not committed** — only this README is tracked.

원시 데이터셋 다운로드 위치. 아래 데이터셋을 이 디렉터리에 받아 캘리브레이션에 사용한다.

- **NASA PCoE Li-ion Battery Data Set** [R7] — OCV·R_int fitting, curve-shape validation, sim-vs-real overlay figure (§8.5)
- **NASA Randomized Usage** [R20] — load-variation robustness (optional)
- **UNIBO Powertools** [R21] — α fitting (5 A vs 8 A) and β fitting (capacity–cycle trajectories)
